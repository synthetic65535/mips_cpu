# verilog_spi

Учебная модель MIPS-подобного процессора, созданная с помощью Verilog.

Для получения осциллограммы используйте следующую последовательность команд:

```cmd
iverilog -f files.txt
vvp a.out
gtkwave out.vcd
```
